<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qinyoucheng.auth.system.mapper.SysMenuMapper">

    <select id="selectMenuTree" resultType="com.qinyoucheng.auth.system.entity.SysMenu">
        SELECT * FROM sys_menu WHERE deleted = 0 AND status = 1
        <if test="appKey != null and appKey != ''">
            AND app_key = #{appKey}
        </if>
        ORDER BY sort ASC, create_time ASC
    </select>

    <select id="selectMenusByRoleId" resultType="com.qinyoucheng.auth.system.entity.SysMenu">
        SELECT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId} AND m.deleted = 0 AND m.status = 1
        ORDER BY m.sort ASC
    </select>

    <select id="selectMenusByUserId" resultType="com.qinyoucheng.auth.system.entity.SysMenu">
        SELECT DISTINCT m.* FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND m.deleted = 0 AND m.status = 1
        ORDER BY m.sort ASC
    </select>

</mapper>
