<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qindashuai.auth.system.mapper.SysRoleMapper">

    <select id="selectRolePage" resultType="com.qindashuai.auth.system.entity.SysRole">
        SELECT * FROM sys_role WHERE deleted = 0
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="appKey != null and appKey != ''">
            AND app_key = #{appKey}
        </if>
        ORDER BY sort ASC, create_time DESC
    </select>

    <select id="selectRolesByUserId" resultType="com.qindashuai.auth.system.entity.SysRole">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.deleted = 0 AND r.status = 1
        ORDER BY r.sort ASC
    </select>

    <select id="hasUsersByRoleId" resultType="boolean">
        SELECT COUNT(1) > 0 FROM sys_user_role WHERE role_id = #{roleId}
    </select>

</mapper>
